# Polar360Panel 実装状況・今後の進め方

方針: 研究用途では「データが確実に取れること」が最優先。
見た目や利便性より先に、データ欠損・データ消失のリスクを潰す実装を優先してきた。

---

## 1. 現状(実装済み)

### 基盤・アプリ構成
- [x] Xcodeプロジェクト(iPad専用、iOS 17以降)
- [x] Polar BLE SDK導入(公式リポジトリのフォーク/パッチ版、GitHub URL参照)
- [x] 起動時に「オンラインモード」「オフラインモード」を選択する画面
- [x] モードごとに完全に別のダッシュボード(2×2の4分割)
- [x] 「センサー管理」画面: センサーIDに名前を付けて管理、保存フォルダ名も名前入りにリネーム
- [x] 「データ管理」画面: 保存済みCSVの一覧・削除・グラフ表示(ズーム/スクロール/範囲固定/日時指定)
- [x] 「このアプリの使い方」画面: モードを分けている理由、設計判断の背景、既知の制約をまとめて解説

### 接続・初期設定
- [x] スキャン→選択→接続、FTU自動実行、`feature_polar_device_control`等の機能有効化
- [x] 再接続の度に: `isFtuDone`確認でFTUスキップ判定、`setLocalTime`で時刻再同期
- [x] アプリ強制終了後も、選んでいたモード・直前のセンサーを記憶して自動再接続
- [x] 「接続」→「計測設定(Hz選択)」→「測定開始」の3段階に分離
- [x] オンラインモードで、以前オフラインで記録中だったセンサーに繋いだ場合の競合検知+確認ダイアログ
- [x] オフラインモードで、既に記録中(切断/モード変更を挟んだだけ)のセンサーは設定画面をスキップして計測継続

### データ取得
- [x] HR: オンライン・オフライン問わず常時ストリーミング+オフライン記録(標準BLE HR Serviceのため両立可能)
- [x] 体表温・加速度: オンラインモードはリアルタイムストリーミング、オフラインモードはセンサー内蔵記録(排他仕様のため)
- [x] 体表温・加速度のサンプルレートを計測設定画面で選択可能(候補はセンサーから実際に取得したものを表示)
- [x] 加速度を「計測しない」選択も可能
- [x] オフライン加速度は10Hzを超えると既知の破損バグ懸念について警告+続行/やり直し選択
- [x] オンラインの加速度・体表温は、通知内の全サンプルをCSV・グラフ両方に反映(取りこぼし修正済み)
- [x] タイムスタンプはセンサー側の正確な値(2000/1/1起点ナノ秒)を使用(HRのみ非対応のため近似)

### 記録の完了・後片付け
- [x] オンライン「計測終了」: ストリーミング停止+切断、グラフを凍結して見返せる状態に。「新しい計測を開始」で同じセンサーに即再接続
- [x] オンライン「切断」: 情報を全部忘れて検索画面に戻る
- [x] オフライン「計測終了」: 記録停止→取得→CSV保存→切断を一括実行、結果サマリー(件数・時間範囲)をアラート表示、OKで検索画面へ
- [x] オフライン「切断」: 接続を切るだけ、記録は継続(意図的な仕様)
- [x] 予期しない切断(電波切れ等)を検知し、ユーザー操作による切断と区別して画面表示・再接続ボタンを提供
- [x] モード変更ボタンは、計測中のパネルがあれば確認ダイアログを挟む

### 記録状況の可視化(オフライン)
- [x] 接続時に記録状況を確認するだけの軽量チェック(停止・取得は行わない)
- [x] 残り容量・残り記録可能時間(推定)を30秒ごとに更新(記録の停止/開始を伴わない安全な方法)
- [x] 前回確認時刻・残り容量(%表示込み)を画面に表示

### 保存・データ管理
- [x] CSV保存(DBではなくCSVのまま。壊れた時の被害範囲・目視確認のしやすさ・既存分析フローとの相性を優先)
- [x] 保存先: `Documents/Online または Offline/<名前_ID>/` の階層、セッション単位でファイル分割
- [x] 接続イベントログ(`events_*.csv`)
- [x] 保存済みデータをアプリ内で閲覧・削除・グラフ表示(ファイルアプリ不要)

### 検証済みの事実
- [x] 充電すると記録は終了する(実測確認済み)
- [x] Polar 360のオフライン加速度記録には、高レート時の既知の破損バグ報告あり(SDK Issue #716, #652)
- [x] SDK Modeを有効にするとHRのストリーミング・記録が両方使えなくなる(採用しない判断の根拠)
- [x] Polar 360のバッテリーは公称最大5日(通常使用時)。本アプリの用途では短くなる可能性が高い

---

## 2. 未実装・今後の課題

### フェーズA: 電池切れ→再充電時の挙動検証(未実施)
充電が記録を止めることは確認済みだが、完全放電→再充電後に
ペアリング・FTU設定・時刻・記録再開がどうなるかは未検証。

- 実際に1台を電池切れさせ、再充電後の挙動を確認する
- 時刻がズレる場合、再接続時の`setLocalTime`で自動補正されることの確認

### フェーズB: 電源投入時の自動記録開始トリガー(未実装)
`setOfflineRecordingTrigger`を使い、アプリが繋がっていない間に電池切れ→再充電が
起きた場合でも、センサー側が自動的に記録を再開する設定。フェーズAの検証結果を踏まえて要否を判断する。

### フェーズC: スケールテスト(未実施)
- 実際に複数センサー・複数iPadでの長時間(数時間〜半日)連続稼働試験
- CSVの日付またぎでの挙動確認(セッション単位のファイル分割のため、日をまたいでも1ファイルのまま続く設計だが、複数日にわたる長時間セッションでの実地確認はまだ)
- 加速度ファイルのFileHandleが長時間の運用に耐えるか

### フェーズD: 運用ドキュメントの整備
- 30台運用時の充電ローテーション表(電池持続時間の実測値をもとに設計)
- センサー故障・工場出荷リセットが必要になった時の対応手順の実地確認

---

## 3. 検証項目 早見表

| # | 検証内容 | 状態 |
|---|---|---|
| 1 | 充電中にオフライン記録が止まるか | ✅確認済み: **充電すると記録は終了する** |
| 2 | 切断中のデータが再接続後に回収できるか | ✅確認済み(計測終了フローで取得・保存まで実装・検証済み) |
| 3 | アプリ強制終了後、自動で元のセンサーに再接続できるか | ✅確認済み |
| 4 | 電池切れ→再充電後、ペアリング/FTU/記録トリガー/時刻がどうなるか | 未実施(フェーズA) |
| 5 | 複数センサー・複数iPadでの長時間安定性 | 未実施(フェーズC) |
| 6 | 違うiPadでのセンサー誤接続 | 理論確認済み(工場出荷リセットが必要という仕様を確認済み) |

---

## 補足: DB化について
CSVのままとする方針で確定(2026年時点)。壊れた時の被害範囲が1ファイルで収まること、
「ファイル」アプリでの目視確認のしやすさ、既存のExcel/Python分析フローとの相性を優先した。
複数センサーを跨いだSQL集計等の具体的ニーズが出てきた場合は、CSV群を後処理でSQLiteに
一括インポートするスクリプトを別途用意する方針(アプリ本体には常時DB書き込みを持たせない)。
