# Polar360Panel 実装状況・今後の進め方

方針: 研究用途では「データが確実に取れること」が最優先。
見た目や利便性(リアルタイムグラフ等)より先に、データ欠損・データ消失のリスクを
潰す実装を優先し、各フェーズの後ろに検証を挟んで進める。

---

## 1. 現状(実装済み)

### 基盤
- [x] Xcodeプロジェクト作成(iPad専用、画面回転は現在フリー)
- [x] Polar BLE SDK 8.1導入(公式リポジトリのネストされたprotocolバグをローカルパッチで回避)
- [x] 2×2の4分割ダッシュボードUI、パネルごとに独立した状態(未接続/スキャン/接続中/データ表示/エラー)

### 接続・初期設定
- [x] スキャン→デバイス選択→接続
- [x] FTU(初回設定)自動実行、正しいasync APIで実装済み
- [x] `feature_polar_device_control`等、必要な機能を正しく有効化(これが無いと`notificationNotEnabled`で詰まることが判明済み)
- [x] 切断(アプリ側の接続を切る)機能

### データ取得
- [x] HRストリーミング → CSV保存(`hr_<deviceId>_<日付>.csv`)
- [x] 体表温ストリーミング(`.skinTemperature`) → CSV保存(`skintemp_<deviceId>_<日付>.csv`)
- [x] 加速度ストリーミング(50Hz) → CSV保存(`acc_<deviceId>_<日付>.csv`)、ファイルハンドルを開きっぱなしにして負荷軽減、定期的にディスク同期
- [x] オフライン記録(HR・体表温)の**開始**処理(切断中もセンサー単体で記録継続)
- [x] オフライン記録の**一覧取得**(件数のみ。中身の取得はまだ)

### ドキュメント
- [x] ペアリング手順フローチャート(工場出荷リセットの注意点込み)
- [x] Xcodeセットアップ手順(SETUP.md)

### 未実装・分かっている論点(TODOのまま)
- [ ] オフライン記録の**取得・CSV保存・削除**(`PolarOfflineRecordingEntry`/`PolarOfflineRecordingData`の型が未確認)
- [ ] アプリ再起動後の自動再接続
- [ ] 再接続の度に必ず行う処理の固定化(FTU済みチェック、自動同期、時刻再設定)
- [ ] 接続イベントログ(いつ繋がって・いつ切れたかの記録)
- [ ] オフライン記録の自動開始トリガー(`setOfflineRecordingTrigger`)
- [ ] リアルタイムグラフ表示
- [x] 充電中に記録が止まるかどうかの検証 → **確認済み: 充電すると記録は終了する**
- [ ] 電池切れ→再充電後の挙動検証(未実施)

---

## 2. 今後の実装順序(提案)

### フェーズ0(すぐやる・実装不要): 充電中の記録停止を検証
**理由を先に検証する。** ここの結果次第で、フェーズ3のトリガー設計が変わるため、
実装より先に済ませておく。

- センサー1台で記録開始 → 充電器に接続 → 数分〜十数分放置 → 充電器から外して同期
- 充電していた時間帯のデータが欠けているか確認

### フェーズ1: オフライン記録の完成(取得・保存・削除)
現状「一覧が取れる」ところまで来ているので、キリのいいここから着手。

1. `PolarOfflineRecordingEntry`/`PolarOfflineRecordingData`の実際の定義を確認(Cmdクリック)
2. 取得(`getOfflineRecord`) → CSV保存 → 削除(`removeOfflineRecord`)を実装
3. **検証**: センサーを数分間アプリから切断した状態にする(電波を遠ざける等) → 再接続 → 「同期」ボタンで、切断中のデータがちゃんとCSVに追記されるか確認

これで「オフラインでも取りこぼさない」という研究要件の核が完成する。

### フェーズ2: 再接続の頑健化
1. 各パネルの最後に繋いだ`deviceId`を`UserDefaults`に保存
2. アプリ起動時、保存された`deviceId`があれば自動で`connectToDevice`を試みる(スキャン画面を経由しない)
3. 再接続の度に必ず行う処理を固定化:
   - `isFtuDone`確認 → 済みならFTUスキップ
   - オフライン記録の自動同期(フェーズ1の処理を手動ボタンでなく自動実行)
   - `setLocalTime`でデバイス時刻を再設定
4. **検証**: アプリを強制終了 → 再起動 → 4パネルとも自動で元のセンサーに繋ぎ直るか確認

### フェーズ3: 接続イベントログ + 自動記録トリガー
1. `events_<deviceId>.csv`に、connected/disconnected/pairingError/ftu_done/offline_syncedのタイムスタンプ付きログを残す
2. フェーズ0の検証結果を踏まえて、`setOfflineRecordingTrigger`で電源投入時に自動記録開始する設定を入れるか判断・実装
3. **検証**: 実際に1台を意図的に電池切れさせ、再充電後の挙動を確認(ペアリング・FTU設定・記録トリガーが生き残るか、時刻がズレるか)

ここまでで「アプリが落ちる」「センサーの電池が切れる」「接続が切れる」といった
研究中に起こりうる異常系に対して、データが失われない設計が一通り揃う。

### フェーズ4: リアルタイムグラフ表示
データの信頼性が固まった後に着手。SwiftUIの`Charts`フレームワークで、
HR/体表温の直近の推移を各パネルに表示する。

### フェーズ5: スケールテスト
1. 実際に複数台のセンサー・複数台のiPadで、長時間(数時間〜半日)の連続稼働試験
2. CSVの日付またぎ(深夜0時)でのファイル切り替わりが正しく動くか確認
3. ACC用ファイルハンドルまわりが長時間耐えられるか確認

### フェーズ6(優先度低・後回しでよい): 細かい後片付け
- `CBCentralManager`の`restoreIdentifier`未設定警告の解消(SDK側パッチが必要、実害なし)
- UIの磨き込み(バッテリー残量表示の見た目など)

---

## 3. 検証項目 早見表

| # | 検証内容 | 実施タイミング | 分かること |
|---|---|---|---|
| 1 | 充電中にオフライン記録が止まるか | ✅確認済み: **充電すると記録は終了する** | 運用ルール: 記録中は絶対に充電しないこと。充電は完全に記録が不要な期間に限定する |
| 2 | 切断中のデータが再接続後に回収できるか | フェーズ1完了後 | オフライン記録機能が実用レベルで動くか |
| 3 | アプリ強制終了後、自動で元のセンサーに再接続できるか | フェーズ2完了後 | 現場でのクラッシュ耐性 |
| 4 | 電池切れ→再充電後、ペアリング/FTU/記録トリガー/時刻がどうなるか | フェーズ3完了後 | 長時間運用での自動復帰可否 |
| 5 | 複数センサー・複数iPadでの長時間安定性 | フェーズ5 | 実際の研究運用に耐えるか |
| 6 | 違うiPadでのセンサー誤接続(既に理論確認済み、余力があれば実機でも確認) | 任意 | 運用ルールの再確認 |

---

## 進め方についての補足

フェーズ0〜3は「データを失わないための土台」なので、ここは飛ばさずに順番通り進めることを勧める。
フェーズ4(グラフ)は魅力的だが、土台が固まる前に着手すると、後から仕様変更の影響を受けやすい。
