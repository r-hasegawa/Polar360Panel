# Polar360Panel ロードマップ（2026年8月時点）

## 1. 記録中の手動イベントマーカー
- **課題**: `CsvLogger.logEvent()`はシステムイベントのみ記録しており、「刺激提示」「タスク開始」等を研究者がタイムスタンプ付きで記録する手段がない。
- **実装方針**: `CsvLogger.logEvent()`を流用し、`OnlineSensorPanelView.swift`/`OfflineSensorPanelView.swift`にボタン+簡易テキスト入力を追加。`SensorSlotViewModel`に`logManualEvent(text:)`を追加。

## 2. 記録データの欠測（ギャップ）自動チェック
- **課題**: `DataManagementView`はファイルサイズ・種別のみ表示し、記録に欠損区間がないか確認する手段がない。
- **実装方針**: `CsvDataLoader.swift`にタイムスタンプ間隔の異常検知を追加し、セッション詳細画面に「最大欠測◯秒」等のバッジを表示。

## 3. バッテリー低下・予期しない切断のローカル通知
- **課題**: ローカル通知の仕組みが皆無。バッテリー低下や予期しない切断は画面を見ていないと気づけない。
- **実装方針**: 通知許可+ローカル通知を発行するマネージャを新設し、`handleDisconnected()`と`batteryLevel`の変化から発火。

---

## 検証タスク

### 電池切れ時のデータ確認
- 完全放電したセンサーで、記録済みデータが失われずに残っているか、再充電・再接続後に吸い出せるかを確認する。

### メモリ満杯時の挙動チェック
- センサー内蔵メモリが満杯になった場合の挙動を確認する（記録が停止するのか、古いデータから上書きされるのか）。満杯後もデータ吸い出しが正常にできるかを確認する。
